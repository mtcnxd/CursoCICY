program UDP;

uses
  Forms,
  UDP_example in 'UDP_example.pas',
  UDP_mainU in 'UDP_mainU.pas' {FrmMain};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFrmMain, FrmMain);
  Application.Run;
end.
